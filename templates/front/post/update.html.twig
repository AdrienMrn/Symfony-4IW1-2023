{% extends 'base.html.twig' %}

{% block title %}Modification de l'y {{ post.content|slice(0, 10) }}[..]{% endblock %}

{% block body %}
    <h1>Modification de l'y {{ post.content|slice(0, 10) }}[..]</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
