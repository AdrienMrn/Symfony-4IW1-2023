{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ tag.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag_index') }}">back to list</a>

    <a href="{{ path('app_tag_edit', {'slug': tag.slug}) }}">edit</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
